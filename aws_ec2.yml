---
plugin: amazon.aws.aws_ec2
regions:
  - eu-central-1
filters:
  instance-state-name:
    - running
keyed_groups:
  - key: tags.Name
    prefix: tag
compose:
 ansible_user: "{{ 'ubuntu' if 'ubuntu' in (tags.Name | default('') | lower) else 'ec2-user' }}"
